# SelfCode - Edit your public frontend via API

How to install and run this project? Just 2 simple commands :

`npm install`

`npm start`

NodeJs dependency is needed !!! After start visit: http://localhost:3000/


